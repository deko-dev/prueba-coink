import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.coink.register',
  appName: 'CoinkApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
